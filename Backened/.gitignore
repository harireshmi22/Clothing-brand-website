# ======================
# 🛑 Backend Ignore Rules
# ======================
# Node.js & Dependencies
node_modules/       # Never commit dependencies!
package-lock.json   # Auto-generated, can be recreated
npm-debug.log*      # NPM error logs
yarn-error.log*     # Yarn error logs

# Environment Variables (SECRETS!)
.env                # Main environment file
.env.local          # Local overrides
.env.development    # Dev environment config
.env.test           # Test environment config
.env.production     # Production config (contains secrets!)

# Build & Cache
dist/               # Compiled/transpiled code
build/              # Build output
.cache/             # Cache folders (Vite, Babel, etc.)
.temp/              # Temporary files

# ======================
# 🛑 Fashion-Mart Ignore Rules
# ======================
# Frontend-specific
/public/assets      # Built assets (can regenerate)
/src/.env           # Frontend env files (if any)

# System & IDE
.DS_Store           # macOS junk
Thumbs.db           # Windows junk
.idea/              # JetBrains IDE files
.vscode/            # VS Code config (personal)

# Logs & Debug
*.log               # All log files
logs/               # Logs directory
tmp/                # Temporary files